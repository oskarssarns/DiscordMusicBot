# LavaLink Louie Bot

## Docker

Create a local environment file from the example and fill in your Discord values:

```bash
cp .env.example .env
```

Run the bot:

```bash
docker compose up --build
```
